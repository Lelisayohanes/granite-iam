spring:
  application:
    name: Granite-IAM

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/granite_iam
    username: granite
    password: granite123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

